# basic configuration
backend = "glx";
vsync = true;

glx-copy-from-front = true;
glx-swap-method = 2;
xrender-sync = true;
xrender-sync-fence = true;

# rounded corners
corner-radius = 4;

# fading
# fading = true;
fade-in-step = 0.1;
fade-out-step = 0.03;

